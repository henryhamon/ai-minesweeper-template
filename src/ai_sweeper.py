# solução final 
def prever_jogada_segura(tabuleiro_atual, modelo):
    # 1. Faça o pré-processamento do tabuleiro_atual (ETL)
    # 2. Peça ao 'modelo' para fazer a predição (.predict)
    # 3. Retorne a coordenada X e Y mais segura
    return (x, y)